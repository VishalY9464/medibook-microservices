server.port=8081
spring.application.name=auth-service

# -- Database - auth-service gets its OWN database ----------------------
spring.datasource.url=jdbc:mysql://localhost:3306/medibook_auth_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Vishal@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true


app.admin.secret.code=AdminSecret2024!

# -- JWT ----------------------------------------------------------------
jwt.secret=medibook_super_secret_key_2026_very_long_string_min_256_bits
jwt.expiration=86400000

# -- Email --------------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yadavvishal9464@gmail.com
spring.mail.password=kifsugzxkqgrjmne
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# -- OAuth2 Google ------------------------------------------------------

spring.security.oauth2.client.registration.google.scope=email,profile

# 🔥 FORCE correct redirect (VERY IMPORTANT)
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# 🔥 ALSO FORCE BASE URL (fixes IP issue completely)
server.forward-headers-strategy=framework


# -- Eureka -------------------------------------------------------------
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# 🔥 MUST BE FALSE (you already fixed ✔)
eureka.instance.prefer-ip-address=false

# 🔥 FORCE hostname
eureka.instance.hostname=localhost

# 🔥 OPTIONAL but STRONG FIX (prevents IP leakage)
eureka.instance.instance-id=${spring.application.name}:${server.port}

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.try-it-out-enabled=true